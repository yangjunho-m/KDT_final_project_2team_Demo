# 비워 두면 same-origin 상대 경로(/api, /ws/realtime)를 사용합니다.
VITE_API_BASE_URL=
VITE_WS_BASE_URL=

# 다른 백엔드를 연결할 때만 로컬 .env에 URL을 지정하세요.
# VITE_DEV_PROXY_TARGET=http://localhost:8000

# VITE_ 값은 브라우저에 공개되므로 서버 비밀키를 넣지 마세요.
VITE_NAVER_MAP_CLIENT_ID=replace_with_naver_map_client_id
